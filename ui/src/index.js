import React from 'react';
import ReactDOM from 'react-dom';
import {default as PresentationRoot } from './components/PresentationRoot';

ReactDOM.render(<PresentationRoot />, document.getElementById('remux'))
